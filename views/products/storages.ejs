<%- include("../partials/header.ejs") %>
<main>
    <div class="container" style="margin-top: 100px; text-align: center;">
        <h1 class="products-h1">Crunch Storage</h1>
        <hr class="style-eight">
    </div>
    <% if(storages.length > 0) { %>
    <div class="container ">
        <div class="row">
            <% for(let storage of storages) { %>
            <div class="col-md-4">
                <div class="card ">
                    <img src="<%= storage.img %>" class="card-img-top storage-image" alt="PIC TEST">
                    <div class="card-body storage-body">
                        <h5 class="card-title"></h5>
                        <p class="card-text">
                            <%= storage.desc %>
                        </p>
                    </div>
                </div>
            </div>
            <% } %>
            <% }else{ %>
        </div>
    </div>
    <div>
        No events
    </div>
    <% } %>
</main>
<%- include("../partials/footer.ejs") %>